/**
 * Local Document Recommendations Page
 */

import { LocalRecommendationDemo } from '@/components/LocalRecommendationDemo';

export default function LocalRecommendationsPage() {
    return (
        <div className="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50">
            <LocalRecommendationDemo />
        </div>
    );
}
